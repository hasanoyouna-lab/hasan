# تسجيل ومتابعة موظفين — فرع عبداللطيف جميل (Pro House)

نظام كشك (kiosk) بيسجّل خروج وعودة الموظفين للبريكات، شغّال على جهاز الكاشير وعلى الجوال.
الهدف الأساسي: **منع التلاعب بالوقت** — الوقت بيتسجّل من ساعة السيرفر لحظة الضغط، مش من
ورقة يكتب فيها الموظف الوقت بنفسه.

- الموقع الحي: https://hasanoyouna-lab.github.io/hasan/
- لوحة الإدارة: `admin.html` (زر الترس بزاوية الهيدر) — كلمة السر بتنطلب كل مرة
- الموظفين حاليًا: هشام، عبدالهادي، غالب

## البنية

موقع ثابت (بدون build ولا npm) + Google Apps Script كـ backend + Google Sheet كقاعدة بيانات.

```
index.html      شاشة الكشك (اختيار الموظف ← السبب ← عدّاد ← عودة)
admin.html      لوحة الإدارة (مين برا الآن / التقرير / الموظفين / الإعدادات)
css/style.css   كل التنسيق. ألوان هوية Pro House: أصفر #F7DC4E، أسود، أحمر #FF5151
js/config.js    رابط الـ API (‏Apps Script ‎/exec)
js/api.js       نداءات GET/POST البسيطة
js/icons.js     أيقونات SVG مربوطة بكلمات مفتاحية (مش بالنص الحرفي للسبب)
js/offline.js   التخزين المحلي وطابور المزامنة
js/kiosk.js     منطق شاشة الكشك
js/admin.js     منطق لوحة الإدارة
sw.js           Service Worker (يشتغل أوفلاين)
assets/fonts/   خط Tajawal محمّل محليًا (٦ ملفات، ~٧٢ كيلوبايت)
apps-script/Code.gs   الباك اند كامل
```

تبويبات الشيت: `Employees` · `Logs` · `Settings`

## قواعد مهمة (انكسرت قبل — لا تكسرها تاني)

1. **`startBreak` لازم يضل idempotent.** طابور الأوفلاين بيعيد إرسال نفس الطلب لحد ما ينجح.
   لو ما فحصنا الـ id الموجود أصلاً، كل إعادة محاولة بتنشئ صف مكرر وبتعيد فتح بريك
   انسكّر. أي تعديل على `startBreak` لازم يحافظ على الفحص بأول الدالة.

2. **لا تقص التاريخ من نص UTC.** `outAt` مخزّن UTC. استخدم `localDayOf(iso, tz)` دايمًا،
   وإلا أي بريك بين منتصف الليل و٣ الصبح بينحسب على اليوم اللي قبله.

3. **مدة البريك لها حد أدنى صفر.** `outAt` مؤجّل بفترة السماح، فلو رجع الموظف خلالها
   بتطلع المدة سالبة وبتنقص من مجموعه اليومي.

4. **الأعمدة الجديدة بتنضاف بآخر `SHEET_HEADERS`** — لو انحشرت بالنص، بيصير عدم تطابق
   بين العناوين والصفوف القديمة.

5. **منطق فترة السماح مكرر عمدًا** بين `graceSecFor` بالسيرفر ونظيرتها بـ `kiosk.js`.
   لو عدّلت وحدة، عدّل التانية — وإلا العدّاد المعروض بيختلف عن الوقت المحفوظ.

6. **ما في أي إشارة لحالة الاتصال بواجهة الموظف** (قرار متفق عليه). المزامنة بتصير بصمت.

## النشر

### الواجهة (GitHub Pages)
`git push` وخلص. **بس** GitHub Pages بيخلي المتصفح يخزّن الملفات ١٠ دقايق، فلازم مع
كل تعديل على CSS/JS تزوّد رقم النسخة بـ ٣ أماكن:

```bash
sed -i 's/?v=13/?v=14/g' index.html admin.html
sed -i 's/ASSET_V = "13"/ASSET_V = "14"/; s/attendance-shell-v13/attendance-shell-v14/' sw.js
```

بدون هاد ممكن يفتح HTML جديد مع JS قديم والصفحة تنكسر.

### الباك اند (Apps Script) — من اللابتوب فقط
```bash
cd apps-script
clasp push --force
clasp deploy --deploymentId <ID> --description "staging"
clasp deploy --deploymentId <ID> --description "الوصف الحقيقي"
```

**لازم نشرتين.** `clasp deploy` مباشرة بعد `push` بينشر النسخة **السابقة** — سلوك معروف
وتأكدنا منه أكثر من مرة (أول نشرة بتطلع بالكود القديم، والتانية بتاخد الجديد).

⚠️ هاي الخطوة بتحتاج `clasp` مسجّل دخول بحساب Google صاحب الشيت — متوفر على اللابتوب بس.
**من جلسة سحابية (جوال) ما بتشتغل**، فتعديلات `Code.gs` لازم تصير من اللابتوب.

## الإعدادات (من لوحة الإدارة، بدون كود)

| الإعداد | الوظيفة |
|---|---|
| `maxBreakMinutes` | الحد الأقصى للبريك الواحد (العدّاد بيصير أحمر بعده) |
| `dailyLimitMinutes` | مجموع كل البريكات المسموح باليوم |
| `breakStartDelaySec` | فترة السماح العامة قبل بدء الاحتساب |
| `reasonGraceSec` | JSON لفترة سماح خاصة لكل سبب، مثلاً `{"حمام":300}` |
| `reasons` | أسباب الخروج مفصولة بفاصلة |
| `adminPassword` | كلمة سر اللوحة |

## أدوات صيانة (GET على رابط الـ API)

| الأمر | الوظيفة |
|---|---|
| `?action=runSetup` | ينشئ/يصلّح التبويبات والأعمدة والإعدادات الافتراضية (آمن للتكرار) |
| `?action=repairDuplicates` | يحذف الصفوف المكررة بنفس الـ id |
| `?action=repairDurations` | يصفّر المدد السالبة |

## ملاحظات

- تعديل مدة بريك أو حذف سجل من تاب "التقرير". البريك المنسي بينتهي من تاب "مين برا الآن".
- أي سجل معدّل يدويًا بينعلّم `adjusted` وبيظهر بعلامة ✎ — مقصود عشان يضل واضح.
- السجلات اللي انحفظت بجهاز بدون نت بتظهر بعلامة 📱 (ساعة الجهاز مش السيرفر).
